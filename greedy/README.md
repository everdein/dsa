# Greedy Algorithms

## What Is Greedy?

A greedy algorithm makes the locally best choice at each step in the hope that it leads to a global optimum.

## Why It Matters

It is often the simplest approach for scheduling, interval, and optimization problems.

## Example

```javascript
function makeChange(coins, amount) {
  // Greedy approach for coin change in some cases
}
```

## Practice Exercises

- Activity selection
- Minimum number of coins
- Interval scheduling
